size(500, 400);
  background(10, 80, 100);
}

void draw() {
  if (mousePressed) {
    background(10, 80, 100);
  } 

stroke(250, 250, 250);
  fill(0, 0, 100);
  ellipse(250, 200, 300, 300);

  fill(150, 0, 0);
  ellipse(250, 200, 70, 70);

  fill(190, 210, 143);
  rect(mouseX, mouseY, 10, 195);

}
